Project 1: Single-Agent Lane Changing (PPO)

Core files:

Single_Agent_Lane_Change_main.py — training entry point

Single_Agent_Lane_Change_environment.py — environment setup + reward shaping

Single_Agent_Lane_Change_ppo_model.py — PPO model (actor–critic)

Single_Agent_Lane_Change_evaluate.py — evaluation script

requirements.txt — dependencies

Documentation:

README_Single_Agent.md

Project 2: Multi-Agent Lane Changing (MAPPO)

Core files:

mappo_main.py — multi-agent training loop

mappo_environment.py — environment configuration

mappo_utils.py — rollout, team reward, GAE

mappo_inference.py — evaluation / inference

requirements.txt — dependencies

Documentation:

README_mappo_upgrade.md

Project 3: Lane Merging (PPO)

train_merge_ppo.py — training script

eval_merge_ppo.py — evaluation script

ppo_merge_final.zip — trained model / final outputs

render_test.py — visualization testing

sanity.py — debugging / checks

